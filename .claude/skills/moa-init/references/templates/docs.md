# 문서 파일 템플릿

## README.md

L1/L2 응답으로 채울 수 있는 항목만 작성한다.
파일 맵·핵심 플로우·주의사항은 코드가 생긴 후 `complete-task` 실행 시 `moa-readme` 스킬이 자동으로 채운다.

```markdown
# {프로젝트 이름}

> {한 줄 요약 — "무엇을 + 어떤 기술로" 한 문장}

## 모아나 봇을 위한 빠른 참조

**이 레포의 책임 영역**

| 도메인 | 위치 |
|--------|------|
{L1 주요 기능 목록 기반으로 도메인 행 작성}

**레포 간 의존성**

{외부 API 연동 선택 시만 작성, 없으면 섹션 전체 생략}

| 방향 | 대상 레포 | 사용 영역 | 이 레포의 진입점 |
|------|----------|----------|----------------|
| 사용함 | — | — | — |

**핵심 도메인 용어**

{개발 진행 후 complete-task 시 moa-readme가 업데이트}

## 파일 맵

{개발 진행 후 complete-task 시 moa-readme가 업데이트}

## 핵심 플로우

{개발 진행 후 complete-task 시 moa-readme가 업데이트}

## 기술 스택

| 레이어 | 기술 |
|--------|------|
| 프론트엔드 | Next.js, TypeScript, Tailwind CSS |
| DB | {Supabase / RDS / 없음} |
| 배포 | {Vercel / EC2} |
{외부 API 연동 시: | 외부 연동 | {서비스명} |}

## 개발 시작

```bash
npm install
npm run dev
```

## 환경변수

| 변수 | 설명 | 기본값 |
|------|------|--------|
{.env.example 항목 기반으로 행 작성}

## 주의사항

{개발 진행 후 complete-task 시 moa-readme가 업데이트}
```

---

## docs/project-overview.md

L1 응답 기반으로 내용을 채운다.

```markdown
# {프로젝트 이름}
> {한 줄 설명}

## 프로젝트 목적 및 배경
- **왜 만들었는가:** {L1 Q3 응답}
- **주요 사용자:** {L1 Q4 응답}

## 주요 기능
{L1 Q5 응답 기반 목록}

> 이 문서는 complete-task 스킬 실행 시 실제 구현 기준으로 업데이트됩니다.
```

---

## docs/infra.md

L2 응답 기반으로 내용을 채운다.

```markdown
# {프로젝트 이름} - 인프라

## 기술 스택
| 분류 | 사용 기술 |
|---|---|
| 프론트엔드 | Next.js, TypeScript, Tailwind CSS |
| 백엔드 | Next.js API Routes |
| DB | {Supabase / RDS / 없음} |
| 배포 | {Vercel / EC2} |
| 외부 연동 | {있으면 추가 / 없으면 없음} |

## 인프라 구조
- 배포 채널: {Vercel / EC2}
- 배포 환경: {운영만 / 개발+운영}
- 도메인 및 운영 URL: (배포 후 업데이트)

## DB 구조
- 사용 DB: {Supabase / RDS / 없음}
- 테이블 및 스키마: (개발 진행에 따라 complete-task가 업데이트)

## 환경변수 목록
| 변수명 | 용도 | 필수 여부 |
|---|---|---|
{.env.example 기반으로 항목 작성}

## 외부 서비스 연동
| 서비스명 | 용도 | 비고 |
|---|---|---|
{외부 API 연동 시 항목 추가 / 아닌 경우 "없음"}

## 연관 프로젝트
| 프로젝트명 | 관계 | 설명 |
|---|---|---|
| | | (complete-task 실행 시 업데이트) |
```

---

## docs/deploy.md

L2 응답 기반으로 내용을 채운다.

```markdown
# {프로젝트 이름} - 배포

## 배포 환경
| 항목 | 내용 |
|---|---|
| 배포 채널 | {Vercel / EC2} |
| 운영 환경 URL | (배포 후 업데이트) |
| 개발 환경 URL | {개발+운영인 경우 / 아니면 "해당 없음"} |

## 배포 방식
- 배포 채널: {Vercel / EC2}
- 운영 브랜치: main
- 개발 브랜치: {개발+운영인 경우 develop / 아니면 "해당 없음"}

## 환경별 설정
| 환경 | 브랜치 | URL |
|---|---|---|
{배포 환경 선택 기반으로 행 작성}

## 배포하는 방법

> 배포는 Claude에게 요청하면 됩니다. Claude가 보안 점검 → 배포 → 결과 확인까지 자동으로 진행합니다.

### 개발 환경 배포
1. Claude Code를 열고 이 프로젝트 폴더에서 시작합니다.
2. 채팅창에 아래 중 하나를 입력합니다:
   ```
   개발 배포해줘
   ```
3. Claude가 배포 전 체크리스트를 확인하고, 문제가 없으면 배포를 진행합니다.
4. 완료되면 개발 환경 URL을 알려줍니다.

### 운영 환경 배포
1. Claude Code를 열고 이 프로젝트 폴더에서 시작합니다.
2. 채팅창에 아래 중 하나를 입력합니다:
   ```
   운영 배포해줘
   ```
3. Claude가 보안 점검을 실행합니다. 문제가 발견되면 배포가 중단되고 조치 방법을 안내합니다.
4. 모든 점검을 통과하면 배포를 진행하고, 완료되면 운영 URL을 알려줍니다.

> **배포가 중단된 경우:** Claude가 안내한 문제를 해결한 뒤 다시 배포를 요청하면 됩니다.

## 배포 전 체크리스트
- [ ] 환경변수 누락 여부 확인
- [ ] 보안 점검 (`moa-security` 스킬 실행)
- [ ] 주요 기능 동작 확인
{Vercel 선택 시: - [ ] Vercel 대시보드 환경변수 설정 확인}
{EC2 선택 시: - [ ] 서버 접속 확인 / - [ ] nginx 설정 확인}

## 배포 이력
| 일시 | 환경 | 배포자 | 주요 변경사항 |
|---|---|---|---|
| | | | (배포 완료 시 자동 기록) |
```

---

## docs/change-log.md

```markdown
# {프로젝트 이름} - 변경 이력

> 최신 변경사항이 상단에 위치합니다.
> complete-task 스킬 실행 시 자동으로 업데이트됩니다.

---

<!-- 변경 이력은 이 아래에 추가됩니다 -->
```
